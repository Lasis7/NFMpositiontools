import { generateSpike } from './postoolclass';

// npx tsx main.ts

// Gotta fix the math related to x_cord. Also seems like the file writing needs to be asynchronous
generateSpike(0, 0, 2625, 0, 'x', 100, 20);
